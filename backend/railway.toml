[build]
builder = "nixpacks"

[deploy]
startCommand = "PYTHONPATH=src python -m distill.main"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
